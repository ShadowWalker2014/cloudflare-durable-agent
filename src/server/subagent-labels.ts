/** Display names for sub-agent types — safe to import from the browser bundle. */
export const SUBAGENT_LABELS: Record<string, string> = {
  researcher: "Researcher",
  writer: "Writer",
  "general-purpose": "General"
};
