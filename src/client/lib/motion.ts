/** One spring for the whole app, so every motion feels the same. https://motion.dev/docs/react-transitions */
export const spring = { type: "spring", stiffness: 420, damping: 32, mass: 0.8 } as const;
